import React from 'react'
import { Button } from 'semantic-ui-react'

const Song = ({ id, title, minutes, seconds, rank, editSong, deleteSong }) => (
  <div>
    this is a song 
  </div>
)

export default Song
